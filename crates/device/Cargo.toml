[package]
name    = "device"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
protocol = { workspace = true }
abi      = { workspace = true }

log       = "0.4"
libc      = "0.2"
thiserror = "1"

[dev-dependencies]
env_logger = "0.11"
